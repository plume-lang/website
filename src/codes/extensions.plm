type User { User(str, int) }

extend show<User> {
  fn show(self) => switch self {
    case User(name, age) => 
      "User { name: " 
        + name 
        + ", age: " 
        + age.show()
        + " }"
  }
}